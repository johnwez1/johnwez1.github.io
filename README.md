# Personal Website

A modern Jekyll site configured for GitHub Pages.

## Run Locally

Install dependencies:

```bash
bundle install
```

Start development server:

```bash
bundle exec jekyll serve
```

Open:

http://localhost:4000

## Deploy

Push to GitHub and enable GitHub Pages under:

Settings → Pages → Deploy from branch
